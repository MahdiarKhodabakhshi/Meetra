/**
 * Section 2 — dark spacer that gives the FloatingLogo scroll room
 * to animate the "Meetra → Meeting the right people" transition.
 * No content of its own — the FloatingLogo renders the text.
 */
export default function ProblemStatement() {
  return (
    <section className="relative bg-[#0A0F1C]" style={{ height: '120vh' }} />
  );
}
